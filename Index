<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hawkesworth LLC - E-commerce Ad Optimization (Professional Polish)</title>
    <!-- Load Google Fonts: Inter only, for consistent typography -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Load Lucide Icons -->
    <script type="module" src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    <style>
        /* Set default font to Inter for all elements, removing Oxanium headers */
        :root {
            font-family: 'Inter', sans-serif;
        }
        /* Removed Oxanium-specific header declarations for a uniform look */
        html {
            scroll-behavior: smooth;
        }

        /* Custom CSS for a clean, deep, professional dark background */
        .professional-background {
            background-color: #0f172a; /* Deep Slate Blue */
            /* Subtle radial light source for depth */
            background-image:
                radial-gradient(at 0% 0%, rgba(30, 58, 138, 0.15) 0%, transparent 70%),
                radial-gradient(at 100% 100%, rgba(23, 37, 84, 0.15) 0%, transparent 60%);
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        /* Hero specific gradient for depth and added subtle data wave pattern */
        .hero-background {
            background-color: #0f172a; /* Base dark color */
            /* Subtle data waves/lines pattern for a technical look */
            background-image:
                repeating-linear-gradient(
                    -45deg,
                    #1e293b 0px,
                    #1e293b 1px,
                    transparent 1px,
                    transparent 40px
                ),
                linear-gradient(to bottom, rgba(15, 23, 42, 1), rgba(0, 0, 0, 0.8));
            background-blend-mode: overlay, normal;
        }
        
        /* --- Hero Entrance Animation (Corporate Look) --- */
        @keyframes corporate-fade-in {
            0% { opacity: 0; transform: translateY(20px); }
            100% { opacity: 1; transform: translateY(0); }
        }
        .hero-reveal {
            opacity: 0;
            animation: corporate-fade-in 0.8s ease-out forwards;
        }
        .hero-reveal.delay-1 { animation-delay: 0.1s; }
        .hero-reveal.delay-2 { animation-delay: 0.3s; }
        .hero-reveal.delay-3 { animation-delay: 0.5s; }

        /* Custom shadow for a polished, elevated look (Toned down and more metallic) */
        .polished-card {
            position: relative;
            overflow: hidden;
            transition: transform 0.3s, box-shadow 0.3s;
            /* Deeper shadow for professional lift, subtle white/metallic inner border for polish */
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.7),
                        inset 0 0 0 1px rgba(255, 255, 255, 0.05);
        }
        .polished-card:hover {
            transform: translateY(-5px);
            /* Subtle outer glow (gold) for interaction, but not overwhelming */
            box-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.9),
                        0 0 15px 3px rgba(245, 184, 65, 0.4), /* Using new cta-gold */
                        inset 0 0 0 1px rgba(255, 255, 255, 0.1);
        }

        /* Enhanced border effect for cards - subtle gradient top edge */
        .polished-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px; /* Slightly thinner */
            background: linear-gradient(90deg, transparent, #F5B841, transparent); /* Using new cta-gold */
            opacity: 0.7;
            transform: translateY(-100%);
            transition: transform 0.4s ease-out;
            z-index: 1;
        }
        .polished-card:hover::before {
            transform: translateY(0%);
        }

        /* Custom animation class for scroll reveal (Fade-in-Up effect) */
        .scroll-reveal {
            opacity: 0;
            transform: translateY(40px);
            transition: opacity 0.8s ease-out, transform 0.8s ease-out;
        }
        .scroll-reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* Style for the integrated process numbers (less circular, more blocky/integrated) */
        .process-step-indicator {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            background-color: #F5B841; /* Gold fill */
            color: #0f172a; /* Dark text */
            border-radius: 8px; /* Slightly rounded block */
            font-size: 1.1rem;
            font-weight: 800;
            box-shadow: 0 4px 10px rgba(0,0,0,0.5);
            margin-bottom: 0.75rem;
            z-index: 10; /* Ensure number stays above line */
        }
        
        /* Methodology Flow Line (Mobile: Vertical Timeline) */
        @media (max-width: 767px) {
            .process-step-container {
                position: relative;
                border-left: 2px solid #334155; /* Vertical line */
                padding-left: 1.5rem;
            }
            .process-step-container > .process-step-indicator {
                position: absolute;
                left: -18px; /* Align to the vertical line */
                top: 0;
                margin-bottom: 0;
            }
            /* Add space between mobile steps */
            .process-step-container:not(:last-child) {
                margin-bottom: 3rem;
            }
        }
        
        /* New CSS for the continuous cycle loop visualization (Desktop only) */
        .continuous-cycle-flow {
            position: relative;
            padding-bottom: 4rem; /* Add space for the loop line */
        }
        .continuous-cycle-flow::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 5%;
            width: 90%;
            height: 40px; /* Curve height */
            /* Draw a curve line */
            border-bottom: 2px solid rgba(245, 184, 65, 0.4);
            border-left: 2px solid rgba(245, 184, 65, 0.4);
            border-right: 2px solid rgba(245, 184, 65, 0.4);
            border-bottom-left-radius: 100%;
            border-bottom-right-radius: 100%;
            transform: translateY(100%); /* Push it slightly below the bottom edge */
            opacity: 0.2;
            pointer-events: none;
            display: none; /* Hidden by default, will show on desktop */
        }

        @media (min-width: 768px) {
            .continuous-cycle-flow::after {
                display: block; /* Show the loop on desktop */
            }
        }
        
        /* Modal Styles */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            z-index: 999;
            display: none;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: opacity 0.3s ease-out;
        }
        .modal-overlay.show {
            display: flex;
            opacity: 1;
        }
        .modal-content {
            transform: scale(0.9);
            transition: transform 0.3s ease-out;
        }
        .modal-overlay.show .modal-content {
            transform: scale(1);
        }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'mechanical-blue': '#1e293b', /* Dark, primary header/card border blue */
                        'metallic-silver': '#e2e8f0', /* Main body text color */
                        'cta-gold': '#F5B841', /* Richer, warmer gold for CTAs/accents */
                        'dark-background': '#0f172a', /* Deepest background color */
                        'card-blue': '#1e293b', /* Card background color */
                        'subtext-gray': '#A0A0A0', /* New neutral gray for secondary text */
                    }
                }
            }
        }
    </script>
</head>
<body class="text-metallic-silver professional-background">

    <!-- Navbar -->
    <header class="sticky top-0 z-50 bg-dark-background/95 backdrop-blur-sm shadow-2xl border-b border-mechanical-blue">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <nav class="flex items-center justify-between h-20">
                <!-- Logo/Branding -->
                <div class="flex-shrink-0">
                    <a href="#" class="flex items-center space-x-2 text-xl font-extrabold text-metallic-silver hover:text-cta-gold transition duration-300">
                        <!-- Original Hawkesworth Logo -->
                        <img src="https://github.com/E-MO36/miles-for-mango/raw/main/HawkesworthLLC.png" 
                             alt="Hawkesworth LLC Logo" 
                             class="w-8 h-8 rounded-full border-2 border-cta-gold shadow-lg"
                             onerror="this.onerror=null; this.src='https://placehold.co/32x32/ffc107/0f172a?text=H';">
                        <span>HAWKESWORTH LLC</span>
                    </a>
                </div>

                <!-- Desktop Nav Links -->
                <div class="hidden md:block">
                    <div class="ml-10 flex items-baseline space-x-6 text-sm font-medium">
                        <a href="#services" class="text-metallic-silver opacity-80 hover:text-cta-gold transition duration-200">Services</a>
                        <a href="#results" class="text-metallic-silver opacity-80 hover:text-cta-gold transition duration-200">Impact</a>
                        <a href="#process" class="text-metallic-silver opacity-80 hover:text-cta-gold transition duration-200">Methodology</a>
                        <!-- CTA button style enhanced for polish -->
                        <button id="desktop-cta-nav" class="px-5 py-2 text-dark-background bg-cta-gold hover:bg-yellow-500 rounded-lg shadow-xl transition duration-300 font-bold transform hover:scale-105">Request Audit</button>
                    </div>
                </div>

                <!-- Mobile Menu Button (Hamburger) -->
                <div class="md:hidden">
                    <button id="mobile-menu-btn" type="button" class="inline-flex items-center justify-center p-2 rounded-md text-metallic-silver hover:text-cta-gold hover:bg-mechanical-blue focus:outline-none focus:ring-2 focus:ring-inset focus:ring-cta-gold" aria-controls="mobile-menu" aria-expanded="false">
                        <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                        <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </button>
                </div>
            </nav>
        </div>

        <!-- Mobile Menu (Hidden by default) -->
        <div class="md:hidden hidden bg-dark-background shadow-xl" id="mobile-menu">
            <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
                <a href="#services" class="text-metallic-silver hover:bg-mechanical-blue hover:text-cta-gold block px-3 py-2 rounded-md text-base font-medium transition duration-200">Services</a>
                <a href="#results" class="text-metallic-silver hover:bg-mechanical-blue hover:text-cta-gold block px-3 py-2 rounded-md text-base font-medium transition duration-200">Impact</a>
                <a href="#process" class="text-metallic-silver hover:bg-mechanical-blue hover:text-cta-gold block px-3 py-2 rounded-md text-base font-medium transition duration-200">Methodology</a>
                <button id="mobile-cta-nav" class="text-dark-background bg-cta-gold hover:bg-yellow-500 block w-full px-3 py-2 rounded-md text-base font-medium mt-2 transition duration-300 text-center">Request Audit</button>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="py-20 sm:py-32 border-b border-gray-700/50 hero-background">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <p class="text-sm font-semibold uppercase text-cta-gold mb-4 tracking-widest hero-reveal delay-1">
                E-COMMERCE PERFORMANCE PARTNERS | OMNICHANNEL STRATEGY
            </p>
            <h1 class="text-4xl sm:text-6xl lg:text-7xl font-extrabold tracking-tight mb-6 leading-tight text-metallic-silver hero-reveal delay-1">
                Unlock <span class="text-cta-gold">Profitable Scale</span> with Data-Driven Advertising
            </h1>
            <p class="text-xl sm:text-2xl text-subtext-gray max-w-4xl mx-auto mb-10 hero-reveal delay-2">
                Hawkesworth LLC partners with you to decode the data and optimize every stage of the digital journey, delivering consistent, exponential Return on Ad Spend (ROAS).
            </p>
            <button id="hero-cta" class="inline-flex items-center justify-center px-10 py-4 border-2 border-cta-gold text-base font-bold rounded-xl shadow-2xl bg-cta-gold text-dark-background hover:bg-yellow-500 transition duration-300 transform hover:scale-[1.03] polished-card hero-reveal delay-3">
                Start Scaling Now
                <span data-lucide="chevrons-right" class="ml-2 w-5 h-5"></span>
            </button>
        </div>
    </section>

    <!-- Services Section -->
    <section id="services" class="py-16 sm:py-24">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-16 scroll-reveal">
                <h2 class="text-3xl sm:text-4xl font-bold text-metallic-silver mb-4">Core E-commerce Advertising Solutions</h2>
                <p class="text-lg text-subtext-gray max-w-3xl mx-auto">
                    We manage the complexities of every major platform, ensuring your products are seen by high-intent customers.
                </p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- Service 1: Marketplace Focus (Amazon/Walmart) -->
                <div class="bg-card-blue p-8 rounded-xl polished-card scroll-reveal" style="transition-delay: 0.1s;">
                    <!-- Updated Icon: package for product/inventory flow across marketplaces -->
                    <span data-lucide="package" class="w-10 h-10 text-cta-gold mb-4 block"></span>
                    <h3 class="text-xl font-bold text-metallic-silver mb-3">Marketplace Strategy & Ad Ops</h3>
                    <p class="text-subtext-gray">
                        Expert management of Amazon Ads (Sponsored Products, Brands, DSP) and Walmart Connect. We build defense and offense campaigns for sustainable growth.
                    </p>
                </div>

                <!-- Service 2: Digital Channel Growth (Google/Meta) -->
                <div class="bg-card-blue p-8 rounded-xl polished-card scroll-reveal" style="transition-delay: 0.2s;">
                    <!-- Original Icon: git-fork retained for cross-channel branching -->
                    <span data-lucide="git-fork" class="w-10 h-10 text-cta-gold mb-4 block"></span>
                    <h3 class="text-xl font-bold text-metallic-silver mb-3">Cross-Channel Demand Generation</h3>
                    <p class="text-subtext-gray">
                        Maximize ROI from Google Shopping and Performance Max, coupled with Meta (Facebook/IG) for full-funnel customer acquisition and remarketing.
                    </p>
                </div>

                <!-- Service 3: Conversion Rate Optimization -->
                <div class="bg-card-blue p-8 rounded-xl polished-card scroll-reveal" style="transition-delay: 0.3s;">
                    <!-- Updated Icon: lightbulb for creative/optimization -->
                    <span data-lucide="lightbulb" class="w-10 h-10 text-cta-gold mb-4 block"></span>
                    <h3 class="text-xl font-bold text-metallic-silver mb-3">Creative & Conversion Optimization</h3>
                    <p class="text-subtext-gray">
                        Data-backed listing optimization, A+ content creation, and landing page testing to ensure high ad spend conversion rates and lower Cost Per Acquisition.
                    </p>
                </div>
            </div>
        </div>
    </section>
    
    <!-- Impact Section (Metrics + Testimonials) -->
    <section id="results" class="py-16 sm:py-24 bg-card-blue border-t border-b border-gray-700/50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-16 scroll-reveal">
                <h2 class="text-3xl sm:text-4xl font-bold text-metallic-silver mb-4">Proven Impact: Data-Driven Efficiency & Growth</h2>
                <p class="text-lg text-subtext-gray max-w-3xl mx-auto">
                    We specialize in delivering high-efficiency, profitable results for scaling e-commerce brands, ensuring every dollar spent works harder.
                </p>
            </div>
            
            <!-- Key Metrics Grid (Visualized with Icons) -->
            <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-16">
                <div class="text-center p-6 bg-dark-background rounded-xl shadow-xl polished-card scroll-reveal" style="transition-delay: 0.1s;">
                    <span data-lucide="trending-up" class="w-8 h-8 text-green-400 mx-auto mb-2 block"></span>
                    <p class="text-4xl font-extrabold text-cta-gold mb-1">7.1x</p>
                    <p class="text-sm font-semibold uppercase text-subtext-gray">Max ROAS Achieved</p>
                </div>
                <div class="text-center p-6 bg-dark-background rounded-xl shadow-xl polished-card scroll-reveal" style="transition-delay: 0.2s;">
                    <span data-lucide="rocket" class="w-8 h-8 text-blue-400 mx-auto mb-2 block"></span>
                    <p class="text-4xl font-extrabold text-cta-gold mb-1">110%</p>
                    <p class="text-sm font-semibold uppercase text-subtext-gray">YOY Revenue Growth</p>
                </div>
                <div class="text-center p-6 bg-dark-background rounded-xl shadow-xl polished-card scroll-reveal" style="transition-delay: 0.3s;">
                    <span data-lucide="banknote" class="w-8 h-8 text-yellow-400 mx-auto mb-2 block"></span>
                    <p class="text-4xl font-extrabold text-cta-gold mb-1">$20,366</p>
                    <p class="text-sm font-semibold uppercase text-subtext-gray">Total Revenue Generated</p>
                </div>
                <div class="text-center p-6 bg-dark-background rounded-xl shadow-xl polished-card scroll-reveal" style="transition-delay: 0.4s;">
                    <span data-lucide="gauge" class="w-8 h-8 text-red-400 mx-auto mb-2 block"></span>
                    <p class="text-4xl font-extrabold text-cta-gold mb-1">$2,881</p>
                    <p class="text-sm font-semibold uppercase text-subtext-gray">Total Ad Spend Managed</p>
                </div>
            </div>
            
            <!-- Testimonials -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-16">
                <!-- Testimonial 1 (Existing) -->
                <div class="bg-dark-background p-8 rounded-xl shadow-2xl polished-card scroll-reveal">
                    <blockquote class="text-center">
                        <span data-lucide="quote" class="w-8 h-8 text-cta-gold mx-auto mb-4"></span>
                        <p class="text-lg italic font-medium text-metallic-silver mb-4">
                            "Hawkesworth LLC completely transformed our Google Shopping campaign. We saw a 150% increase in qualified traffic and our ROAS climbed from 2.1x to 4.8x in just six months."
                        </p>
                        <footer class="text-sm font-semibold text-subtext-gray">
                            — Sarah L., VP of E-commerce, <span class="font-bold text-metallic-silver">AquaTech Gear</span>
                        </footer>
                    </blockquote>
                </div>
                <!-- Testimonial 2 (New Mock) -->
                <div class="bg-dark-background p-8 rounded-xl shadow-2xl polished-card scroll-reveal" style="transition-delay: 0.2s;">
                    <blockquote class="text-center">
                        <span data-lucide="quote" class="w-8 h-8 text-cta-gold mx-auto mb-4"></span>
                        <p class="text-lg italic font-medium text-metallic-silver mb-4">
                            "Their focus on Walmart Connect delivered results faster than we anticipated. Within the first quarter, our advertising ACOS dropped by 28% while revenue increased 65%."
                        </p>
                        <footer class="text-sm font-semibold text-subtext-gray">
                            — Mark R., Founder, <span class="font-bold text-metallic-silver">Global Home Goods</span>
                        </footer>
                    </blockquote>
                </div>
            </div>

            <!-- Client Logos (Mock) - REMOVED for cleaner presentation -->
        </div>
    </section>

    <!-- Process Section (Methodology Flowchart) -->
    <section id="process" class="py-16 sm:py-24">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-16 scroll-reveal">
                <h2 class="text-3xl sm:text-4xl font-bold text-metallic-silver mb-4">Our Methodology: The Continuous Growth Cycle</h2>
                <p class="text-lg text-subtext-gray max-w-3xl mx-auto">
                    A robust, data-first operating cycle built for transparent, repeatable, and scalable success.
                </p>
            </div>

            <div class="relative flex flex-col md:flex-row justify-between space-y-12 md:space-y-0 md:space-x-12 continuous-cycle-flow">
                <!-- Flow Line with Arrows (Desktop Only) -->
                <div class="hidden md:block absolute inset-0 flex items-center justify-center pointer-events-none z-0">
                    <div class="w-11/12 h-1 bg-gray-700/50 relative">
                        <!-- Arrow 1 (Audit -> Launch) -->
                        <div class="absolute -top-3 left-1/3 transform -translate-x-1/2 text-cta-gold">
                            <span data-lucide="chevron-right" class="w-6 h-6 transform scale-150"></span>
                        </div>
                        <!-- Arrow 2 (Launch -> Analyze) -->
                        <div class="absolute -top-3 right-1/3 transform translate-x-1/2 text-cta-gold">
                            <span data-lucide="chevron-right" class="w-6 h-6 transform scale-150"></span>
                        </div>
                    </div>
                </div>

                <!-- Step 1: Audit & Strategize -->
                <div class="relative flex-1 p-8 bg-card-blue rounded-xl shadow-xl polished-card text-center scroll-reveal z-10 process-step-container" style="transition-delay: 0.1s;">
                    <div class="process-step-indicator mx-auto">1</div>
                    <span data-lucide="search" class="w-8 h-8 text-metallic-silver mx-auto mb-3 block"></span>
                    <h3 class="text-xl font-bold text-cta-gold mb-2">Audit & Strategize</h3>
                    <p class="text-subtext-gray">
                        Comprehensive account audit, competitor analysis, and market segmentation to define precise, high-ROAS campaign structures.
                    </p>
                </div>

                <!-- Step 2: Launch & Optimize -->
                <div class="relative flex-1 p-8 bg-card-blue rounded-xl shadow-xl polished-card text-center scroll-reveal z-10 process-step-container" style="transition-delay: 0.2s;">
                    <div class="process-step-indicator mx-auto">2</div>
                    <span data-lucide="zap" class="w-8 h-8 text-metallic-silver mx-auto mb-3 block"></span>
                    <h3 class="text-xl font-bold text-cta-gold mb-2">Launch & Optimize</h3>
                    <p class="text-subtext-gray">
                        A/B testing creative and copy, optimizing product feeds, and deploying advanced, platform-specific bidding algorithms for efficiency.
                    </p>
                </div>

                <!-- Step 3: Analyze & Scale -->
                <div class="relative flex-1 p-8 bg-card-blue rounded-xl shadow-xl polished-card text-center scroll-reveal z-10 process-step-container" style="transition-delay: 0.3s;">
                    <div class="process-step-indicator mx-auto">3</div>
                    <span data-lucide="line-chart" class="w-8 h-8 text-metallic-silver mx-auto mb-3 block"></span>
                    <h3 class="text-xl font-bold text-cta-gold mb-2">Analyze & Scale</h3>
                    <p class="text-subtext-gray">
                        Weekly transparent reports, deep-dive performance reviews, and continuous budget reallocation to lock in winning strategies and scale aggressively.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Final CTA Banner (New Section) -->
    <section class="py-16 bg-cta-gold text-dark-background text-center">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 scroll-reveal">
            <h2 class="text-3xl sm:text-4xl font-bold mb-4">Ready to Outpace the Competition?</h2>
            <p class="text-xl opacity-90 mb-8">
                Request your complimentary E-commerce Performance Audit today.
            </p>
            <button id="footer-cta" class="inline-flex items-center justify-center px-10 py-4 border-2 border-dark-background text-base font-bold rounded-xl shadow-2xl bg-dark-background text-cta-gold hover:bg-gray-800 transition duration-300 transform hover:scale-[1.05]">
                Get Your Free Audit
                <span data-lucide="rocket" class="ml-2 w-5 h-5"></span>
            </button>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-dark-background text-metallic-silver py-10 border-t border-gray-700/50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
                <!-- Brand Info -->
                <div class="col-span-2 md:col-span-1">
                    <a href="#" class="text-2xl font-extrabold text-metallic-silver mb-3 block">HAWKESWORTH LLC</a>
                    <p class="text-sm text-subtext-gray">Performance Marketing for E-commerce.</p>
                    <p class="text-sm mt-4 text-subtext-gray">Elevating brands through data-led advertising since <span id="start-year">2018</span>.</p>
                </div>
                <!-- Quick Links -->
                <div>
                    <h4 class="text-lg font-semibold text-metallic-silver mb-4">Quick Links</h4>
                    <ul class="space-y-2 text-sm text-subtext-gray">
                        <li><a href="#services" class="hover:text-cta-gold transition duration-200">Our Services</a></li>
                        <li><a href="#results" class="hover:text-cta-gold transition duration-200">Client Impact</a></li>
                        <li><a href="#process" class="hover:text-cta-gold transition duration-200">Our Methodology</a></li>
                    </ul>
                </div>
                <!-- Platforms -->
                <div>
                    <h4 class="text-lg font-semibold text-metallic-silver mb-4">Platforms</h4>
                    <ul class="space-y-2 text-sm text-subtext-gray">
                        <li>Amazon Advertising</li>
                        <li>Google Ads (PMax/Shopping)</li>
                        <li>Meta (Facebook/IG)</li>
                        <li>Walmart Connect</li>
                    </ul>
                </div>
                <!-- Contact (Simplified) -->
                <div>
                    <h4 class="text-lg font-semibold text-metallic-silver mb-4">Connect</h4>
                    <ul class="space-y-2 text-sm text-subtext-gray">
                        <li><a href="mailto:info@hawkesworth.com" class="hover:text-cta-gold transition duration-200">info@hawkesworth.com</a></li>
                        <li><a href="#" class="hover:text-cta-gold transition duration-200">Privacy Policy</a></li>
                        <li><a href="#" class="hover:text-cta-gold transition duration-200">Careers</a></li>
                    </ul>
                </div>
            </div>

            <div class="border-t border-gray-700/50 mt-10 pt-6 text-center text-sm opacity-60">
                &copy; <span id="current-year"></span> Hawkesworth LLC. All rights reserved.
            </div>
        </div>
    </footer>
    
    <!-- Contact Modal (Hidden Pop-up) -->
    <div id="contact-modal" class="modal-overlay">
        <div class="modal-content w-full max-w-lg mx-4">
            <div class="bg-card-blue p-8 rounded-xl shadow-2xl polished-card relative">
                <!-- Close Button -->
                <button id="close-modal-btn" class="absolute top-4 right-4 text-metallic-silver hover:text-cta-gold transition duration-200 p-2 rounded-full bg-dark-background/50">
                    <span data-lucide="x" class="w-6 h-6"></span>
                </button>

                <div class="text-center mb-8">
                    <h2 class="text-3xl font-bold text-cta-gold mb-2">Request Your Free Audit</h2>
                    <p class="text-subtext-gray">
                        Let's analyze your current performance and map out your path to 7.1x ROAS.
                    </p>
                </div>

                <!-- Contact Form -->
                <form id="modal-form" class="space-y-4">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <input type="text" placeholder="Your Name" required class="w-full px-4 py-3 border border-gray-700 rounded-lg bg-dark-background text-metallic-silver focus:ring-cta-gold focus:border-cta-gold">
                        <input type="email" placeholder="Work Email" required class="w-full px-4 py-3 border border-gray-700 rounded-lg bg-dark-background text-metallic-silver focus:ring-cta-gold focus:border-cta-gold">
                    </div>
                    <input type="text" placeholder="Company Website / Store URL" class="w-full px-4 py-3 border border-gray-700 rounded-lg bg-dark-background text-metallic-silver focus:ring-cta-gold focus:border-cta-gold">
                    <textarea placeholder="Tell us about your current advertising challenges and monthly ad spend..." rows="4" required class="w-full px-4 py-3 border border-gray-700 rounded-lg bg-dark-background text-metallic-silver focus:ring-cta-gold focus:border-cta-gold"></textarea>
                    <button type="submit" class="w-full px-4 py-3 font-bold text-lg text-dark-background bg-cta-gold rounded-xl hover:bg-yellow-500 transition duration-300 transform hover:scale-[1.01] shadow-xl">
                        Submit Audit Request
                    </button>
                    <p id="modal-form-message" class="text-center text-sm pt-2 text-green-400 font-semibold hidden">Success! We'll review your submission and contact you within one business day.</p>
                </form>
            </div>
        </div>
    </div>


    <!-- JavaScript for Mobile Menu, Year, Form, and Scroll Reveal -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // --- Modal Logic ---
            const modal = document.getElementById('contact-modal');
            const form = document.getElementById('modal-form');
            const formMessage = document.getElementById('modal-form-message');
            const closeBtn = document.getElementById('close-modal-btn');
            const ctas = [
                document.getElementById('desktop-cta-nav'),
                document.getElementById('mobile-cta-nav'),
                document.getElementById('hero-cta'),
                document.getElementById('footer-cta')
            ];

            const showModal = () => {
                modal.classList.add('show');
            };

            const hideModal = () => {
                modal.classList.remove('show');
                // Reset form state on close
                form.reset();
                formMessage.classList.add('hidden');
            };

            // Event listeners for all CTAs
            ctas.forEach(cta => {
                if (cta) cta.addEventListener('click', showModal);
            });
            
            closeBtn.addEventListener('click', hideModal);
            
            // Hide modal if user clicks outside of the content (on the overlay)
            modal.addEventListener('click', (e) => {
                if (e.target.id === 'contact-modal') {
                    hideModal();
                }
            });

            // Form submission handler
            form.addEventListener('submit', (e) => {
                e.preventDefault();
                // Simulating a successful submission
                form.reset();
                formMessage.classList.remove('hidden');

                // Auto-close modal after success message is shown
                setTimeout(() => {
                    hideModal();
                }, 4000);
            });


            // --- General Page Logic ---

            // Mobile Menu Toggle Logic
            const menuBtn = document.getElementById('mobile-menu-btn');
            const mobileMenu = document.getElementById('mobile-menu');
            const menuLinks = mobileMenu.querySelectorAll('a');

            const toggleMenu = () => {
                const isExpanded = menuBtn.getAttribute('aria-expanded') === 'true';
                menuBtn.setAttribute('aria-expanded', !isExpanded);
                mobileMenu.classList.toggle('hidden');
            };

            menuBtn.addEventListener('click', toggleMenu);

            // Close menu when a link is clicked
            menuLinks.forEach(link => {
                link.addEventListener('click', () => {
                    if (!mobileMenu.classList.contains('hidden')) {
                        toggleMenu();
                    }
                });
            });

            // Set Current Year in Footer
            document.getElementById('current-year').textContent = new Date().getFullYear();

            // --- Intersection Observer Logic for Scroll Reveal ---
            const observer = new IntersectionObserver((entries, observer) => {
                entries.forEach((entry) => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        observer.unobserve(entry.target); // Stop observing once visible
                    }
                });
            }, {
                threshold: 0.1 // Trigger when 10% of the element is visible
            });

            // Target all elements with the .scroll-reveal class
            document.querySelectorAll('.scroll-reveal').forEach((el) => {
                observer.observe(el);
            });
        });

        // Initialize Lucide Icons
        if (typeof lucide !== 'undefined') {
            lucide.createIcons();
        }
    </script>
</body>
</html>
